{
  "name": "@dragic/database",
  "version": "0.0.1",
  "description": "Database layer with Drizzle ORM for Dragic monorepo",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rimraf dist",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "drizzle-kit migrate",
    "db:push": "drizzle-kit push",
    "db:studio": "drizzle-kit studio"
  },
  "dependencies": {
    "drizzle-orm": "^0.44.4",
    "pg": "^8.16.3"
  },
  "devDependencies": {
    "drizzle-kit": "^0.30.6",
    "@types/pg": "^8.11.6",
    "typescript": "^5.1.3",
    "rimraf": "^5.0.5"
  },
  "peerDependencies": {
    "@nestjs/common": "^10.0.0"
  }
}
